"""Detection layer — behavioral signal extraction.

Import from submodules directly (e.g., ``from gradata.detection.addition_pattern
import AdditionTracker``). Package-level re-exports were removed — all callers
already used submodule paths.
"""
