# Session Summary — 2026-03-22 (S37)

## Session Type
Systems (Sunday night)

## What Was Done
1. **Skill stocktake cleanup** — Fixed 43 skills/gates (34 IMPROVE + 9 MERGE). KEEP count: 61 → 102. 5 oversized skills trimmed by 984 lines (30% reduction). 8 domain gates got frontmatter. 4 merge pairs differentiated with NOT boundaries.
2. **System wiring audit (10-point inspection)** — Found 8 gaps, 5 efficiency issues. Wrap-up validator exposed as rubber stamp (8 event types had 0 data, 100% pass rate = measurement blindness).
3. **Event system fixed** — OUTPUT events: 0 → 24 (backfilled) + PostToolUse hook live. DELTA_TAG: 0 → 31 (backfilled from activity_log) + auto-emits. GATE_RESULT: PostToolUse hook wired (8 events already). COST_EVENT: Stop hook fixed (Windows compat + event emission). TOOL_FAILURE: New PostToolUse hook for MCP error detection. STALE_DATA: Added to session_start_reminder.py. HALLUCINATION: Piggybacks on ACCURACY corrections. GATE_OVERRIDE: Helper function for explicit overrides.
4. **Confidence scoring rewrite** — Replaced blanket +0.10 with evidence-based scoring. Queries CORRECTION events, matches against lesson categories. -0.15 for failures, +0.10 only when earned. lesson_applications table wired.
5. **Data pipeline** — Follow-Up Tracker: Dataview → materialize_tracker.py (machine-readable). daily_metrics: 2 → 12 rows (backfilled). Session types fixed: 2 "full" → 9 "full". Snapshot save-minimal command added.
6. **5 new automation hooks** — session-init-data.js (snapshot + tracker + API sync), output-event.js (OUTPUT events on prospect writes), gate-emit.js (GATE_RESULT on gate reads), session-close-data.js (auto confidence scoring), tool-failure-emit.js (MCP error detection).
7. **SDK portability** — 21 scripts rewired from hardcoded paths to paths.py imports. 0 hardcoded paths remain outside paths.py. SDK.md contract written. Environment variable overrides for all paths.
8. **Direct API integration (zero Claude dependency)** — api_sync.py: Pipedrive, Gmail, Calendar, Instantly, Fireflies. SessionStart hook runs sync automatically. Startup/wrap-up SKILL.md updated to read from sync results, not re-query MCP. Google OAuth completed. 5/5 sources live and tested.
9. **Prospect data sync** — 18 prospect files synced to loop-state truth. Jon Ambach (Bright Choice) created. Dan Farmer (Staples) closed-lost (bounced). Fireflies intel added to 4 prospect files. Lead sources added to 14 prospects.
10. **Wrap-up validator** — Check 16 added (delta sync freshness). Now 16 checks total.
11. **Cleanup** — 5 v1 orphan SQLite tables dropped. emit_step() duplicate removed from wrap_up.py. backfill_tags.py + install_brain.py archived. .carl/sessions/ removed.

## Outcomes Logged
- No pipeline outcomes (systems session)

## Learnings Applied
- Event system: measurement requires actual data — 100% pass with 0 data is blindness, not quality
- SDK portability: hardcoded paths are a deployment blocker — centralize in paths.py with env var overrides
- Confidence scoring: blanket increments mask real performance — evidence-based scoring from CORRECTION events

## Oliver's Summary
Massive infrastructure overhaul to bulletproof the system before feeding sales data Monday. Skill stocktake cleaned up, event system wired with real data, confidence scoring made evidence-based, direct API sync built for all 5 sales tools (zero Claude dependency), SDK portability achieved. All hooks fixed for Windows. System ready for pipeline work.

## Corrections Received
0 corrections this session. Oliver directed, agent executed.

## Open Items
- startup-brief.md over 80 lines (was 87 from S36, needs trimming)
- 34 IMPROVE skills remain (mostly third-party) — address during pipeline sessions
- 9 MERGE pairs differentiated but not fully consolidated

## Rule Upgrade Candidates
- Paths centralization (paths.py pattern) — strong candidate for RULE promotion given 21 scripts rewired with 0 regressions
- Evidence-based confidence scoring — replaces blanket +0.10, should be permanent

## Stats
- Skills fixed: 43 (34 IMPROVE + 9 MERGE)
- Lines trimmed: 984 (30% reduction on 5 oversized skills)
- Events backfilled: 55 (24 OUTPUT + 31 DELTA_TAG)
- New hooks: 5
- Scripts rewired to paths.py: 21
- API sources integrated: 5 (Pipedrive, Gmail, Calendar, Instantly, Fireflies)
- Prospects synced: 18
- Wrap-up validator checks: 16 (was 15)
- Corrections: 0
