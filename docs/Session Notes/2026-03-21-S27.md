# Session 27 — 2026-03-21 — SDK Architecture + RAG Intelligence + System Hardening

## User Summary
Oliver asked for three categories of work: (1) steal best practices from Claude Code innovation to improve the agent, (2) build out the SDK/marketplace vision with strategic files, quality standards, layer classification, and extraction prep, (3) harden the existing system with wikilinks, wiring audits, and RAG upgrades. All executed as direct instructions — no corrections needed. Confident throughout since these were build-to-spec tasks. Best output: the layer classification audit and config.py split — clean separation with zero breakage. Least sure about: whether the episodic memory decay rate (0.008/day) is tuned right — only testable with more time spread.

## What Was Done

### Hooks & Innovation (Claude Code Best Practices)
- Created PreCompact hook (pre-compact-snapshot.js) — auto-snapshots brain state before context window resets
- Created PostToolUseFailure hook (post-tool-failure.js) — deterministic fallback chain reminders on tool failure
- Added worktree isolation pattern to CLAUDE.md parallel agent rule
- Confirmed embedding model already on gemini-embedding-2-preview, ran full re-embed

### RAG Intelligence Upgrades
- Episodic memory: recency weighting in query.py (recent sessions scored higher, 0.8%/day decay, 50% floor)
- Confidence scoring: similarity bands (high/medium/low) flagged on every result
- Outcome linking: embed.py now parses PATTERNS.md proven angles + learnings/ outcome tags into vector metadata
- config.py expanded with recency/confidence/outcome config sections
- Full re-embed successful: 94 chunks from 63 files

### SDK / Marketplace Vision
- Created brain/vault/sdk-north-star.md — strategic boundary (Sprites Work vs [NAME])
- Created .claude/commands/sdk-audit.md — SDK contamination scanner (CONTAMINATED/REVIEW/CLEAN)
- Appended Marketplace Quality Standard — 4-signal composite (corrections, outcomes, wikilinks, time spread)
- Appended Platform Universality — brain layer vs runtime layer definition + extraction folder structure
- Ran marketplace readiness assessment: corrections healthy (96), outcomes half-built, wikilinks thin (70→107), time spread critical (5 days vs 90 minimum)

### Layer Split Execution
- Created paths.py (RUNTIME) — all hardcoded paths extracted, env var overrides
- Cleaned config.py (BRAIN) — zero hardcoded paths, pure portable config
- Updated start.py to import from paths.py
- Created /runtime/ skeleton (claude-code, cursor, api, template)
- Full layer classification audit saved to brain/vault/
- All imports verified clean, embed pipeline confirmed working

### System Hardening
- Created .claude/periodic-audits.md — 4 recurring audits (/sdk-audit every 5, memory cleanup every 10, RAG freshness every 3, config bloat every 10)
- Wired periodic audits into session-start Phase 0.5 step 9
- Added Environment section to CLAUDE.md (Windows, paths, PowerShell)
- Added SDK Architecture section to CLAUDE.md (brain/runtime layer rule)
- Replaced "Starter SDK"/"the SDK" with [NAME] placeholders in vault files
- Ran full wiring integrity audit: zero broken references confirmed
- Bulk wikilink pass: 37 links added across 28 files (70→107 total, 32→1 zero-link files)

### Component Map & Changelog
- Updated component-map.md: Periodic Audits section, RAG Pipeline descriptions refreshed
- 6 changelog entries added (all with SDK-portable/runtime classification)

## Outcomes Logged
- Systems session — no tactic→result outcomes to log

## Learnings Applied
- No corrections received. No queued learnings.

## Open Items
- periodic-audits.md last_run values all at S0 — will start tracking next session
- Gemini rate limit may throttle queries immediately after full re-embed
- Wikilink density improved but still ~1.7/file — will compound naturally with prospect sessions
- Time spread for marketplace: 85 days remaining before Apprentice tier (mid-June 2026)
- /log-outcome system exists but has zero formal entries — needs use in prospect sessions

## Rule Upgrade Candidates
- None — no corrections this session
