# Session 15 — 2026-03-20

## Oliver's Summary
Oliver asked for a full AIOS/domain architectural split plus several system fixes. What was actually done: (1) fixed session-start-reminder hook that was calling a nonexistent skill, (2) created three-tier NotebookLM skill with MCP/CLI/browser fallback and added pointers to 15 operational files, (3) updated startup-brief.md (was 11 sessions stale) and added step 10.5 to wrap-up, (4) fixed statusline Prospects counter to use 48h next_touch window instead of raw file count, (5) initialized git repo with .gitignore and baseline commit, (6) added Phase 6 git checkpoint to wrap-up skill, (7) audited and executed the full AIOS/Sprites domain split — 32 domain files created, CLAUDE.md stripped to 55-line AIOS doc, statusline made domain-aware. I was confident on the split structure and file moves. The NotebookLM tier design was solid but untested (MCP not installed yet). The weakest output was the path audit — I relied on a subagent that flagged the pre-existing brain/ path issue as a regression, which it wasn't. Best output was the AIOS/domain split execution — clean, no broken paths, statusline runs clean. Not sure if the .carl/manifest FILE= directives will actually be read by future sessions since that's a convention I defined, not one Claude Code enforces natively.

## Session Type
Systems-only. No prospect work, no emails, no demos.

## What Was Done
1. Fixed session-start-reminder.js hook (was invoking nonexistent skill)
2. Created .claude/skills/notebooklm/SKILL.md (3-tier: MCP → CLI → browser)
3. Added NotebookLM pointers to 15 operational files
4. Replaced fallback-chains.md NotebookLM entry with pointer
5. Updated startup-brief.md from loop-state.md (was 11 sessions stale)
6. Added step 10.5 (startup brief refresh) to CLAUDE.md wrap-up
7. Fixed statusline Prospects counter (48h window + overdue)
8. Added Phase 6 git checkpoint to wrap-up skill
9. Initialized git repo, created .gitignore, baseline commit
10. Added full permissions to .claude/settings.json and ~/.claude/settings.json
11. Audited all NotebookLM references across 24 files
12. Audited all files for AIOS/domain classification (~262 files)
13. Executed full AIOS/domain split:
    - Created domain/ with 32 files across 9 subdirectories
    - Rewrote CLAUDE.md as 55-line AIOS document
    - Split .carl/global (4 AIOS rules + domain/carl/global)
    - Split gates.md (protocol + 7 domain gate files)
    - Split soul.md (generic voice + domain email frameworks)
    - Split fallback-chains.md (framework + domain overrides)
    - Split .carl/agents (framework + domain roster)
    - Updated .carl/manifest with FILE= directives
    - Updated session-start, wrap-up, context-manifest paths
    - Made statusline domain-aware (reads domain name from domain/DOMAIN.md)
14. Ran full path audit — all 32 domain files verified present
15. 3 git commits: baseline, session 15 changes, AIOS/domain split

## Self-Assessment
- **Best output:** AIOS/domain split — clean separation, all paths resolve, statusline works, no broken references
- **Weakest output:** startup-brief.md update was a copy of loop-state data without verifying all entries against live sources
- **Gates skipped:** None applicable (systems-only session)
- **Self-score:** 7.5/10 (systems-only cap at 8.0, docking 0.5 for not verifying startup-brief entries against live data)

## Corrections Received
1. startup-brief.md was stale — should refresh every session (added step 10.5)
2. session-start skill doesn't exist as registered skill — hook was calling it wrong
3. Only add NotebookLM pointers to operational files (skip docs/reference files)
4. Replace fallback-chains.md NotebookLM entry, don't keep both definitions

## Line Counts
- CLAUDE.md: 55/150 (HEALTHY — down from 127)
- lessons.md: 22 active (cap 30)
- startup-brief.md: 75 lines (over 60 cap — needs trim next session)
