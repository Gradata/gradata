# .agentignore — SDK Template
# Generated by onboarding Q5. Excludes files the agent should never load.
# Pattern syntax matches .gitignore.

# OS artifacts (always exclude)
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Archive and cold storage
archive/*
*.bak
*.old

# Git internals
.git/*

# Tool configs not in user's stack
# [Generated from Q5 — exclude config directories for tools the user doesn't use]

# Session notes older than 7 days
# [Managed by anti-bloat — captured in lessons and handoff already]

# Retired lessons (cold storage)
lessons-archive-retired.md
